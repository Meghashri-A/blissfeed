<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            background-image: url("home2.jpg");
            background-size: cover;
            background-position: center;
            position: relative;
            overflow: hidden;
        }

        .container {
            max-width: 500px;
            width: 100%; /* Set width to 100% */
            padding: 20px;
            border-radius: 5px;
            background-color: rgba(0, 0, 0, 0.5); /* Black transparent overlay */
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.5); /* Adding shadow effect */
            position: relative; /* Ensure proper stacking context */
            z-index: 2; /* Ensure container appears above the background */
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #fff; /* White text color */
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: #fff; /* White text color */
        }

        input[type="text"],
        input[type="password"],
        input[type="email"] {
            width: calc(100% - 22px);
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 3px;
            background-color: rgba(255, 255, 255, 0.8); /* White background with transparency */
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #007bff; /* Blue color */
            color: white;
            border: none;
            border-radius: 3px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }

        .message {
            text-align: center;
            color: red;
            position: relative;
            margin-top: 10px; /* Adjust the margin to position the message */
        }

        .success {
            color: green;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Register</h1>
        <p class="message">
            <%= message %>
        </p>
        <form id="registerForm" action="/register" method="POST">
            <label for="regUsername">Username:</label>
            <input type="text" id="regUsername" name="username" required>
            <label for="regPassword">Password:</label>
            <input type="password" id="regPassword" name="password" required>
            <label for="regEmail">Email:</label>
            <input type="email" id="regEmail" name="email" required><br><br>
            <button type="submit">Register</button>
            <div id="registerMessage" class="message"></div>
        </form>
        <p style="color: #fff;">Already have an account? <a href="/login" style="color: gold">Login here</a></p>
    </div>
</body>

</html>
