<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            background-image: url("home2.jpg");
            background-size: cover;
            background-position: center;
            position: relative;
            overflow: hidden;
        }

        .container {
            max-width: 500px;
            padding: 20px;
            border-radius: 5px;
            background-color: rgba(0, 0, 0, 0.5); /* Black transparent overlay */
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.5); /* Adding shadow effect */
            position: relative; /* Ensure proper stacking context */
            z-index: 2; /* Ensure login container appears above the background */
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #fff; /* White text color */
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: #fff; /* White text color */
        }

        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ced4da;
            border-radius: 3px;
            background-color: rgba(255, 255, 255, 0.8); /* White background with transparency */
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 3px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #0056b3;
        }

        .error {
            text-align: center;
            color: red;
            position: relative;
            margin-top: 20px; /* Adjust the margin to position the error message below h1 */
        }

        .success {
            text-align: center;
            color:green;
            position: relative;
            margin-top: 20px; /* Adjust the margin to position the error message below h1 */
        }

        /* Animation */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .animated {
            animation: fadeIn 1s ease-in-out;
        }
    </style>
</head>

<body>

    <div class="container animated">
        <h1 class="mb-4">Login</h1>
        <!-- Display success/error messages using Bootstrap alert components -->
        <div class="<%= status %>">
            <%= message %>
        </div>
        <form id="loginForm" action="/login" method="POST">
            <div class="form-group">
                <label for="loginUsername">Username:</label>
                <input type="text" id="loginUsername" name="username" class="form-control" required>
            </div>
            <div class="form-group">
                <label for="loginPassword">Password:</label>
                <input type="password" id="loginPassword" name="password" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-primary">Login</button>
        </form>
        <p class="mt-3" style="color: #fff;">Don't have an account? <a href="/register" style="color:goldenrod">Register here</a></p>
    </div>

    <!-- Bootstrap JS (optional) -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
